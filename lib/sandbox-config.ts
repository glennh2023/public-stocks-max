// ---------------------------------------------------------------------------
// SANDBOX CONFIGURATION
//
// This is a LIMITED SANDBOX from StocksMax Research. It is a demo build
// prepared specifically for club/application review. It contains sample
// components and tools only — it is NOT the full StocksMax Research project,
// which remains private and is not licensed for redistribution.
// ---------------------------------------------------------------------------

// Simple access gate for the deployed demo. This is a convenience gate to keep
// casual visitors out of a shared deployment — it is client-side and NOT a
// security boundary (the code ships in the bundle). Change it before deploying.
export const ACCESS_CODE = "Quant-Illinois-2026";

export const SANDBOX_NAME = "StocksMax Research — Limited Sandbox";

export const SANDBOX_NOTICE =
  "Limited sandbox build. This demo contains sample components and tools " +
  "from StocksMax Research, prepared for application review only. It is not " +
  "the full StocksMax Research project.";
